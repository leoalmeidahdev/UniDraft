/**
 * Placeholder até o projeto Supabase existir de verdade.
 * Depois de rodar as migrations (`supabase db push` ou `npm run db:push`),
 * gere o tipo real com:
 *
 *   npx supabase gen types typescript --project-id <id> > src/types/database.types.ts
 *
 * As queries de dados do app usam o Drizzle (src/lib/db) como fonte de verdade
 * tipada; este tipo só é usado pelo `supabase-js` para Auth/Realtime.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
